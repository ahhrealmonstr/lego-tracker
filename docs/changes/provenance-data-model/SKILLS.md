# Recommended Skills: Collection & Provenance Data Model

> Auto-generated by Pipeline Skill Advisor, then annotated with per-skill rationale.
> Scanned 760 skills in 306ms. Signals: 0 keywords, 1 stack marker, 5 domains.

## Reference (load as context)

- **`db-temporal-data`** — during implementation, relevance 0.35.
  Maps to **D1**. `collection_events` is temporal data, and the acquisition-vs-current-state
  split is the bitemporal problem in miniature: one fact about the past, one about now.

- **`test-snapshot-patterns`** — testing, relevance 0.40.
  Maps to **SC4**. The v1→v2 migration is asserted against fixture collections, which is
  snapshot testing by another name.

## Consider

- **`api-idempotency-keys`** — during implementation, relevance 0.32.
  Maps to **SC9**. The `(item_id, type, at, md5(payload))` unique constraint *is* an
  idempotency key protecting event replay.

- **`test-integration-patterns`** — testing, relevance 0.35.
  **SC6**, **SC7**, **SC8**, and **SC10** are cross-device sync behaviours that unit tests
  cannot reach.

- **`test-tdd-workflow`** — testing, relevance 0.35.
  Implementation Order steps 1–2 are pure logic, deliberately sequenced to be TDD-able before
  persistence exists.

- **`harness-test-data`** — testing, relevance 0.34.
  Fixture collections for the migration transform, including malformed records.

- **`design-design-documentation`** — during implementation, relevance 0.33.
  Two ADRs are required (D1, D7).

## Notes

`db-temporal-data` and `api-idempotency-keys` are the two worth reading before writing code —
both map to specific success criteria rather than to general good practice.

The advisor matched on stack and domain signals only (**0 keyword matches**), so these are
weaker recommendations than the scores suggest. Treat them as prompts, not direction.
